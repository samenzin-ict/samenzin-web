/**
 * Development placeholder. The public site is built in a later phase-1 session;
 * see ROADMAP.md. Deliberately not user-facing content, so no CMS strings here.
 */
export default function HomePage() {
  return (
    <main>
      <h1>samenzin-web</h1>
      <p>
        Development scaffold. The admin panel is at <a href="/admin">/admin</a>.
      </p>
    </main>
  )
}
