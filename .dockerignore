# Anything not needed to build the image. Keeping this tight makes the build
# faster and keeps local secrets out of the build context.
.env
.env.*
!.env.example

node_modules
.next
.git
.github

media
docs
*.pdf

.devseed
.vscode
.idea
